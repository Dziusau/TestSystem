2„то будет в результате выполнени€ операции 2 + 2 == 5 && 12 / 4 == 3 || 2 == 5 % 3?
true
2јбстрактный класс может не содержать ни одного абстрактного метода (да/нет)?
да
2ћетоды в интерфейсе могут иметь тело, а могут не иметь (да/нет)?
да
2јбстрактный метод может иметь тело, а может не иметь (да/нет)?
нет
2ћожно ли использовать try блок без блока catch или finally (да/нет)?
нет
2ћожет ли один блок catch перехватывать несколько исключений?
да
2явл€ютс€ ли объекты классов StringBuffer и StringBuilder измен€емыми?
да
2 акой размер в байтах занимает тип int (прим: 1 байт)?
4 байта
2Ѕудет ли catch блок обрабатывать подклассы указанного исключени€ (да/нет)?
да
2 акие исключени€ обрабатываютс€ в блоке try-catch?
контролируемые и неконтролируемые
2 акой размер в байтах занимает тип float (прим: 1 байт)?
4 байта
2 акой класс не переопредел€ет методы equals() и hashCode(), наследу€ их непосредственно от класса Object?
java.lang.StringBuilder
2 акой размер в байтах занимает тип double (прим: 1 байт)?
8 байт
2Ќазовите основной целочисленный тип.
int
2 акой размер в байтах занимает тип short (прим: 1 байт)?
2 байта
1Ќайдите соответствие:/1. a++/а. условный оператор/2. ++a/б. постфиксна€ форма инкремента/3. if()/в. префиксна€ форма инкремента
1б2в3а
1Ќайдите соответствие:/1. false/а. литерал типа boolean/2. 1F/б. размер типа long/3. 16 байт/в. переменна€ типа float
1а2в3б
2„то означает ключевое слово void?
не возвращает значение
